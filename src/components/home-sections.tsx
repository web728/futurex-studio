// Kept only so existing `from "./home-sections"` imports elsewhere in the
// project don't break. This was a duplicate of cinematic-sections.tsx —
// both now point at the single source of truth in ./cinematic/*.
export * from "./cinematic";
