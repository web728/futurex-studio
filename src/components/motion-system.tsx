// Kept only so existing `from "./motion-system"` imports elsewhere in the
// project don't break. All real code now lives in ./motion/*. New code


// should import from "@/components/motion" directly.
export * from "./motion";

